# -*- coding: utf-8 -*-
"""组合：底部反包择时（bottom_reversal）跑在上证指数场外基金上 —— 为了和资产配置组合同图对比

这个文件**没有新策略**：规则全部来自 quant/strategies/bottom_reversal.py（plans/09），
这里只做两件事：
1. 换标的：上证指数 → **富国上证指数ETF联接A（100053）**，支付宝能直接买的场外基金
   （用户没有 A股账户，指数本身买不了；联接基金跟踪 上证综指ETF 510210，
   与指数误差约 -0.02%/年，见 Knowledge/funds.md）
2. 换契约：用 quant/adapter.py 的 strategy_as_portfolio 把"进/出布尔信号"翻译成
   "买多少钱/卖多少钱"，于是能进组合引擎、能和 longterm_balance 同图比净值。

为什么要绕这一圈（对比的公平性问题）：
- 择时策略大部分时间空仓（21 笔 × 最多持 30 日，8 年里约 1/3 时间在场），
  资产配置组合永远满仓。放在同一个引擎里跑，**空仓期的现金不生息会如实计入净值**，
  比的就是"同一笔钱从头到尾交给谁"这个真问题，而不是"在场那段谁选点准"。
- 起点也必须对齐：run.py 的组合比选模式会自动取各配方"成分就绪日"的最晚那个，
  否则"开始得早"本身就是优势。

必须知道的口径（都在 quant/adapter.py 的头部详述）：
1. 成交价是 **T+1 净值**（组合引擎口径），单标的引擎是 T+1 开盘价——基金一天只有
   一个净值（open=close），所以两边同价，数字应当对得上（2026-07-28 已核对：
   两边都是 +49.5%，年化 5.11% vs 5.1%，残差来自成本扣法的舍入）。
2. min_hold 自动提到 5 个交易日（基金 <7 自然日赎回费 1.5% 的防线）。
3. **赎回费没有精确模拟**：本回测双边各 0.1%，但 A 类实盘是"申购 ~0.15% +
   持有 7天~1年赎回 0.5%"，一次往返约 0.65% —— 30 日持仓的策略这笔费用很重。
   想看真实摩擦：run.py 里把 COST 调到 0.003 再跑一遍对比（结论若翻转，
   说明这个策略在场外基金上根本落不了地）。实操该买的是 C 类（013286，
   持有 >7 天免赎回费），但 C 类 2021-08 才成立，长区间回测只能用 A 类。
4. 指标预热段：靠下面的 data_start="20110101" 往前留历史，组合引擎会用回测起点
   之前那段给指标暖机（2026-07-28 修，此前会漏掉开头几周的信号）。
"""
from quant import Portfolio
from quant.adapter import strategy_as_portfolio

PORTFOLIO = Portfolio(
    name="bottom_reversal_fund",
    holdings={"上证指数联接A": "fund:100053"},   # 富国上证指数ETF联接A（支付宝可买）
    decide_fn=strategy_as_portfolio("bottom_reversal", fund_mode=True),
    data_start="20110101",                        # 该基金 2011-01-30 成立
    note="规则见 quant/strategies/bottom_reversal.py；2018-07~2026-07 实测年化 +5.11%、"
         "回撤 -11.9%，与单标的引擎跑同一只基金的结果一致（+49.5%）。"
         "但同期这只基金买入持有 +75.4%（回撤 -21.3%）→ 择时没赚到钱，只换来更浅的回撤",
)
